{
  "name": "diffgeo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "An interactive differential geometry engine for curves and surfaces in R\u00b3 (after do Carmo)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "oracle": "npm run oracle:emit && python3 scripts/oracle/verify.py",
    "oracle:emit": "vite-node scripts/oracle/emit.ts"
  },
  "dependencies": {
    "katex": "^0.18.4"
  },
  "devDependencies": {
    "@types/katex": "^0.16.8",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vite-node": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
