{
  "name": "diffgeo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "An interactive differential geometry engine for curves and surfaces in R³ (after do Carmo)",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "oracle": "python3 scripts/oracle/verify.py"
  },
  "dependencies": {
    "katex": "^0.18.4"
  },
  "devDependencies": {
    "@types/katex": "^0.16.8",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10"
  }
}
